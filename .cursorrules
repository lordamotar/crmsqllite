# 📌 Задача: Разработать CRM-систему для обработки заказов

## Сохранение текущего функционала

- Не ломать существующее поведение: любые изменения — только добавления/исправления без удаления фич.
- сключение: явное удаление мёртвого кода/полей — только в рамках соответствующих задач.
- зменения должны быть минимальными и по делу, без побочных «улучшений» вне scope задачи.

## Стандарты кода

- PEP 8: соблюдение стиля и форматирования Python-кода.
- Понятные имена: осмысленные названия переменных, функций, модулей.
- Короткие функции: одна ответственность, простая читаемость, избегать глубокой вложенности.
- Логирование: использовать стандартный модуль `logging` (уровни DEBUG/INFO/WARNING/ERROR), не логировать чувствительные данные.
- Единообразие: следовать текущему стилю проекта; внешние зависимости — только при реальной необходимости.
## 🎯 Цель
Создать веб-систему для обработки заказов с функционалом:
- Добавление и редактирование заказов.
	поля: 	дата закза(автомат),	Телефон из тблицы clients,	Клиент(физ/юр) из тблицы clients,	ФИО/(ТОО) из тблицы clients,	Номенклатура,	Цена,	Кол-во,	Сумма,	Сегмент(из списка),	Город филиала(из списка),	Город клиента(из списка) из тблицы clients,	Статус заказа,	Ответственный из тблицы users,	Откуда заказ(из списка),	Способ оплаты(из списка),	По акции -10% на сайте,	Номер заказа,	Номер реализации,	Дополнительно,											
- Управление клиентами и товарами.
- Смена статусов заказов.
- Многопользовательский доступ с ролевой моделью.
## 🛠 Технический стек
- Backend: **Django** + **Django REST Framework**
- Все приложения в apps/
- Frontend: **Bootstrap**
- БД: **PostgreSQL**
- Авторизация: Django Auth + JWT (роли: admin, manager, accountant)
- Реальное время (по желанию): Django Channels (WebSocket)
## 🎨 UI компоненты (React)
- Login Page (JWT Auth)
- Dashboard (Список заказов + фильтры)
- Order Form (создание/редактирование)
- Clients Page
- Products Page
- User Management Page (для admin)

разработка в 2 этапа: 1) backend раездели каждый apps отдельно,  2) frontend.
недобавлять новые функции в backend если они не нужны и пока я не скажу.
миграции создавать буду я сам только давай мне инструкции как их создавать.
Минимизировать объем, комментировать функции. 
Хранить экспортируемые данные в локальной базе PostgreSQL.
Используй MCP context7.
Отвечай на русском языке.

Я не хочу пушистой подкормки. Мне нужна правда, пусть даже она режет, если это необходимо для роста. Давай полный, не отфильтрованный анализ, даже если он жесток, даже если он ставит под сомнение мои решения, мышление, поведение или направление. Посмотри на мою ситуацию с полной объективностью и стратегической глубиной.

Я хочу, чтобы ты указал, что я делаю неправильно, что недооцениваю, чего избегаю, какие отговорки использую и где трачу время впустую или играю в мелочь. А затем скажи, что конкретно мне нужно сделать, о чём подумать или что построить, чтобы действительно перейти на следующий уровень с точностью, ясностью и беспощадным расставлением приоритетов. Если я заблудился – скажи прямо.

Если я совершаю ошибку – объясни, в чём она. Если я на правильном пути, но двигаюсь слишком медленно или с неверной энергией – расскажи, как это исправить. Не сдерживайся. Относись ко мне как к человеку, чьё будущее зависит от правды, а не от нежного утешения.
