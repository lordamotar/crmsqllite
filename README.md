# CRM Project-A — Django/DRF + Bootstrap

**Статус:** Backend готов, Frontend на Bootstrap, PostgreSQL как единственный источник данных, JWT авторизация реализована.

**Текущая версия:** v1.0 - Полнофункциональная CRM система для обработки заказов с автоматическим заполнением полей товаров.

---

## Технический стек
- **Backend:** Django 5.2.6 + Django REST Framework
- **База данных:** PostgreSQL (локальная)
- **Авторизация:** Django Auth + JWT (роли: admin, manager, accountant)
- **Frontend:** Bootstrap 5 + JavaScript (AJAX)
- **Кэширование:** Django LocMemCache
- **Оптимизация:** Database indexes, select_related, prefetch_related

## Структура проекта
- **`apps/`** — Django приложения:
  - `accounts/` — пользователи и роли
  - `clients/` — управление клиентами
  - `products/` — каталог товаров
  - `orders/` — заказы и их обработка
  - `cities/` — города и филиалы
  - `dashboard/` — главная панель
  - `user_profile/` — профили пользователей
- **`templates/`** — HTML шаблоны с Bootstrap
- **`static/`** — CSS, JS, изображения
- **`config/`** — настройки Django

## Быстрый старт

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Настройка базы данных
Создайте PostgreSQL базу данных:
```sql
CREATE DATABASE projecta_db;
CREATE USER aikos_super WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE projecta_db TO aikos_super;
```

### 3. Миграции
```bash
python manage.py migrate
```

### 4. Создание суперпользователя
```bash
python manage.py createsuperuser
```

### 5. Запуск сервера
```bash
python manage.py runserver
```

Откройте http://127.0.0.1:8000 в браузере.

## Основной функционал

### ✅ Реализовано
- **Управление заказами:** создание, редактирование, просмотр списка
- **Автоматическое заполнение полей:** при вводе кода товара поля заполняются автоматически
- **Управление клиентами:** CRUD операции с клиентами
- **Каталог товаров:** импорт и управление товарами
- **Многопользовательский доступ:** роли admin, manager, accountant
- **Оптимизация производительности:** кэширование, индексы БД, оптимизированные запросы
- **AJAX поиск:** динамический поиск товаров без перезагрузки страницы

### 🎯 Ключевые особенности
- **Умный поиск товаров:** по коду или названию с автозаполнением
- **Многоуровневые цены:** розничная, оптовая, акционная
- **Цветовая индикация:** статусы заказов и способы оплаты
- **Адаптивный интерфейс:** Bootstrap 5 для всех устройств
- **Быстрая работа:** оптимизированные запросы и кэширование

## API Endpoints

### Аутентификация
- `POST /api/auth/jwt/create/` — создание JWT токена
- `POST /api/auth/jwt/refresh/` — обновление токена
- `POST /api/auth/jwt/verify/` — проверка токена

### Заказы
- `GET /orders/` — список заказов с фильтрацией
- `POST /orders/add/` — создание заказа
- `GET /orders/<id>/edit/` — редактирование заказа
- `GET /orders/product-search/?q=<query>` — поиск товаров (AJAX)

### Клиенты
- `GET /clients/` — список клиентов
- `POST /clients/add/` — добавление клиента
- `GET /orders/client-lookup/?q=<phone>` — поиск клиента по телефону (AJAX)

## Производительность

### Оптимизации
- **Database indexes** на часто используемые поля
- **select_related** и **prefetch_related** для уменьшения N+1 запросов
- **Кэширование** фильтров и статических данных (5 минут)
- **Ограничение** количества товаров в формах (1000 записей)
- **AJAX поиск** вместо статических списков

### Мониторинг
- Логи Django в `logs/django.log`
- Консольные сообщения для отладки JavaScript
- Профилирование запросов через Django Debug Toolbar (в разработке)

## Импорт данных

### Товары
```bash
python manage.py universal_import --file path/to/products.xlsx --type products
```

### Клиенты
```bash
python manage.py universal_import --file path/to/clients.xlsx --type clients
```

Подробные инструкции в файлах:
- `IMPORT_PRODUCTS_README.md` — импорт товаров
- `FINAL_PRODUCTS_SYSTEM.md` — структура системы товаров
- `PRODUCTS_IMPORT_SUMMARY.md` — сводка импорта

## Разработка

### Структура приложений
Каждое приложение в `apps/` содержит:
- `models.py` — модели данных
- `views.py` — представления (views)
- `urls.py` — маршруты
- `admin.py` — админ-панель
- `templates/` — HTML шаблоны
- `migrations/` — миграции БД

### Стиль кода
- PEP 8 для Python
- Понятные имена переменных и функций
- Короткие функции с одной ответственностью
- Логирование через стандартный модуль `logging`
- Единообразие с текущим стилем проекта

## Лицензия
Внутренний проект. Распространение по договорённости.



